source 'https://rubygems.org'

gemspec :name => 'image_optim_pack'

git 'https://github.com/toy/image_optim.git', :branch => 'guetzli' do
  gem 'image_optim'
end

if RUBY_VERSION >= '2.0'
  gem 'travis_check_rubies', '~> 0.2'
end
